[package]
name = "vate"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = { version = "1.11.0", optional = true }
vate-derive = { path = "../vate-derive" }

[dev-dependencies]
once_cell = "1.20.1"

[features]
full = [
    "regex",
]
regex = ["dep:regex"]